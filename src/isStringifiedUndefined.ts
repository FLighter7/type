/**
 * Checks that a value is `'undefined'` (as string)
 *
 * @param {unknown} val any value
 * @returns {boolean}
 */
export const isStringifiedUndefined = (val: unknown): boolean => val === 'undefined';
