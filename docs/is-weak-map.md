### isWeakMap

```js
/*
isWeakMap(val: unknown): boolean
*/

/**
 * Checks that a value is a WeakMap instance
 * Uses "val instanceof WeakMap" comparison
 */

// Examples
isWeakMap(new WeakMap());// true
```
