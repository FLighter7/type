### isWeakSet

```js
/*
isWeakSet(val: unknown): boolean
*/

/**
 * Checks that a value is a WeakSet instance
 * Uses "val instanceof WeakSet" comparison
 */

// Examples
isWeakSet(new WeakSet());// true
```
