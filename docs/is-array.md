### isArray

```js
/*
isArray(val: unknown): boolean
*/

/**
 * Checks that a value is an array
 * Uses "Array.isArray"
 */

// Examples
isArray([]);// true
isArray({});// false
isArray(new Array());// true
```
